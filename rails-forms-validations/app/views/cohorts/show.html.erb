<h1><%= @cohort.name %></h1>

<p>These are our students:</p>

<ul>
  <% @cohort.students.each do |student| %>
    <li><%= link_to student.name, student %></li>
  <% end%>
</ul>
<a href="/cohorts/<%= @cohort.id %>/edit">Edit</a>

<%= link_to "Delete this!", cohort_path(@cohort), method: :delete, data: {confirm: "Are you sure?"} %>
